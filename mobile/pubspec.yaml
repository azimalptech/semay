name: semay
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  # Firebase is kept for FCM push ONLY — all data/auth/storage/functions moved
  # to the self-hosted API (see docs/07_MIGRATION.md Phase 9).
  firebase_core: ^4.12.0
  firebase_messaging: ^16.4.2
  image_picker: ^1.2.3
  video_player: ^2.11.1
  video_thumbnail: ^0.5.6
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1
  flutter_svg: ^2.2.4
  url_launcher: ^6.3.1
  image_cropper: ^11.0.0
  share_plus: ^11.0.0
  visibility_detector: ^0.4.0+2
  scrollable_positioned_list: ^0.3.8
  dio: ^5.10.0
  web_socket_channel: ^3.0.3
  flutter_secure_storage: ^10.3.1
  sqflite: ^2.4.3
  path: ^1.9.1
  uuid: ^4.6.0
  connectivity_plus: ^7.3.1

# firebase_messaging 16.4.2 declares firebase_core_platform_interface ^7.1.0, but
# its compiled code extends `FirebasePlugin`, which only exists from 8.0.0 onward
# (7.1.0 has only `FirebasePluginPlatform`). That is a broken constraint in the
# published package, so the override is load-bearing: without it `pub get`
# resolves happily and then the Dart build fails with "Type 'FirebasePlugin' not
# found". Remove only after upstream ships a fixed constraint AND an Android build
# actually succeeds without it — `flutter pub get` passing proves nothing here.
dependency_overrides:
  firebase_core_platform_interface: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  flutter_launcher_icons: ^0.14.4

# flutter_launcher_icons config — run `dart run flutter_launcher_icons` to
# (re)generate every platform icon size from assets/app_icon.png.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/app_icon.png"
  remove_alpha_ios: true
  # Android adaptive icon: same flat white background + centered wordmark,
  # split into its own foreground/background layers so the OS can mask it
  # into a circle/squircle/etc. without cropping the text (the plain PNG
  # path above still covers pre-8.0 devices that don't support adaptive
  # icons at all).
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/app_icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/illustrations/
    - assets/logo.png

  # Geist (Vercel, SIL Open Font License 1.1 — see assets/fonts/Geist-OFL.txt).
  # Every text style in the Figma file specifies Geist; the app previously set no
  # fontFamily at all and rendered in Roboto, which was the single largest visual
  # difference from the design. Only the weights the design actually uses are
  # bundled — Regular 400, Medium 500, SemiBold 600 — plus Bold 700 for emphasis.
  fonts:
    - family: Geist
      fonts:
        - asset: assets/fonts/Geist-Regular.ttf
          weight: 400
        - asset: assets/fonts/Geist-Medium.ttf
          weight: 500
        - asset: assets/fonts/Geist-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Geist-Bold.ttf
          weight: 700

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
