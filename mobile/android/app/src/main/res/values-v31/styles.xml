<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ (API 31) only. The OS here shows a MANDATED system splash
         that, by default, renders the app's launcher icon — the same logo the
         Dart SplashScreen (lib/features/auth/splash_screen.dart) shows a moment
         later, so the user saw the logo pop TWICE ("two splash screens").
         Blanking the icon (a transparent drawable) + matching the background
         makes this first, system splash an imperceptible color flash with no
         logo, so the logo appears exactly once. Light-mode variant; the dark
         one lives in values-night-v31/styles.xml. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background_color</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_transparent</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
